import Container, { Button } from "./style"

export default () => (
  <Container>
    <Button>
      <button></button>
      <span>SELECT</span>
    </Button>
    <Button>
      <button></button>
      <span>START</span>
    </Button>
  </Container>
)
